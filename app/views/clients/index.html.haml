%p Whether it was graphics or a full production, we have done work for these people on some capacity over the last few years. Rollover below for some testimonials.

- @clients.each_with_index do |client, index|
  .client{data: {name: client.name, description: client.description }, class: ('active' if index == 0)}
    = image_tag "clients/#{client.slug}.png"



.row
  .description
    %h3#client-name= @clients.first.name
    %p#client-description= @clients.first.description

/ %h1 Listing clients

/ %table
/   %tr
/     %th Ordinal
/     %th Name
/     %th Slug
/     %th Description
/     %th
/     %th
/     %th

/   - @clients.each do |client|
/     %tr
/       %td= client.ordinal
/       %td= client.name
/       %td= client.slug
/       %td= client.description
/       %td= link_to 'Show', client
/       %td= link_to 'Edit', edit_client_path(client)
/       %td= link_to 'Destroy', client, :method => :delete, :data => { :confirm => 'Are you sure?' }

/ %br

/ = link_to 'New Client', new_client_path
